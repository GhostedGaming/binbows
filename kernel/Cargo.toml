[package]
name = "limine-rust-template"
version = "0.1.0"
edition = "2024"

[dependencies]
limine = "0.5"
binbows-interrupts = { workspace = true }
binbows-logging ={ workspace = true}
binbows-gdt ={ workspace = true}
